{% extends 'base.html.twig' %}

{% block title %}Réinitialiser le mot de passe{% endblock %}

{% block body %}
{% include 'header.html.twig' %}
<div class="mx-4">
    <p class="mb-5">Réinitialiser votre mot de passe ici!</p>
    {{ form_start(form) }}
        <div class="form-group">
            <label for="exampleInputEmail1">{{ form_label(form.password.first) }}:</label>
            <div>{{ form_widget(form.password.first, { 'attr': {'class': 'form-control'} }) }}</div>
            <small id="emailHelp" class="form-text text-muted">{{ form_help(form.password.first) }}</small>
        </div>
        <div class="form-group">
            <label for="exampleInputEmail1">{{ form_label(form.password.second) }}:</label>
            <div>{{ form_widget(form.password.second, { 'attr': {'class': 'form-control'} }) }}</div>
            <small id="emailHelp" class="form-text text-muted">{{ form_help(form.password.second ) }}</small>
        </div>
        <button class="btn btn-primary" type="submit">Modifiez</button>
    {{ form_end(form) }}
</div>
{% include 'footer.html.twig' %}
{% endblock %}