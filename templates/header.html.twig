<header class="header">
    <ul class="navbar navbar-dark bg-primary d-flex justify-content-around" >  
        <li class="nav-item list-inline-item">
        <a class="navbar-brand text-focus-in" href="{{ path('home') }}">Accueil</a></li>
        <li class="nav-item list-inline-item">
        <a class="navbar-brand text-focus-in" href="http://localhost:8000/#trick">Tricks</a></li>
        {% if is_granted('ROLE_SUPER_ADMIN') %}
            <li class="nav-item list-inline-item">
            <a class="navbar-brand text-focus-in" href="{{ path('app_user') }}">Utilisateurs</a>
        {% endif %}
        {% if app.user %}
            <li class="nav-item list-inline-item">
            <a class="navbar-brand text-focus-in" href="{{ path('app_logout') }}">Déconnexion</a></li>
        {% else %}
            <li class="nav-item list-inline-item">
            <a class="navbar-brand text-focus-in" href="{{ path('app_login') }}">Connexion</a></li>
            <li class="nav-item list-inline-item">
            <a class="navbar-brand text-focus-in" href="{{ path('app_register') }}">Inscription</a></li>
        {% endif %}
    </ul>
</header>